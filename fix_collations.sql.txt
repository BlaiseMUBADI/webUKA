-- ============================================================================
-- CORRECTION DES COLLATIONS - compte_agent et t_membre_jury
-- ============================================================================
-- Description: Uniformise les collations des colonnes Login et Mat_agent
--              pour correspondre aux nouvelles tables de sessions
-- ============================================================================

USE bdd_uka;

-- ----------------------------------------------------------------------------
-- Modifier la collation de compte_agent.Login
-- ----------------------------------------------------------------------------
ALTER TABLE `compte_agent` 
MODIFY COLUMN `Login` VARCHAR(45) CHARACTER SET utf8mb3 COLLATE utf8mb3_general_ci NOT NULL;

-- ----------------------------------------------------------------------------
-- Modifier la collation de compte_agent.Mot_passe
-- ----------------------------------------------------------------------------
ALTER TABLE `compte_agent` 
MODIFY COLUMN `Mot_passe` VARCHAR(255) CHARACTER SET utf8mb3 COLLATE utf8mb3_general_ci NOT NULL;

-- ----------------------------------------------------------------------------
-- Modifier la collation de t_membre_jury.Login
-- ----------------------------------------------------------------------------
ALTER TABLE `t_membre_jury` 
MODIFY COLUMN `Login` VARCHAR(50) CHARACTER SET utf8mb3 COLLATE utf8mb3_general_ci DEFAULT NULL;

-- ----------------------------------------------------------------------------
-- Modifier la collation de t_membre_jury.Mot_passe
-- ----------------------------------------------------------------------------
ALTER TABLE `t_membre_jury` 
MODIFY COLUMN `Mot_passe` VARCHAR(255) CHARACTER SET utf8mb3 COLLATE utf8mb3_general_ci DEFAULT NULL;

-- ============================================================================
-- FIN DU SCRIPT
-- ============================================================================

SELECT 'Collations modifiées avec succès!' AS Message;
SELECT 'Tables mises à jour: compte_agent, t_membre_jury' AS Info;
